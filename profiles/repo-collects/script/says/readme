0: 基础 (元封装 语言端 理念的描述与应用)
1: 平台 (设施封装 运行端 平台的提供)
2: 数据管理 (里封装 后台端 数据库)
3: 业务应用 (表封装 界面端 小软件)

一个仓库可能存在于多个中，也可能有的可以存在于多个中但却只被记录于一个中。

越往上的重要程度越高。

一个仓库可以从一个挪到另一个。


我们的工作是什么？只有一件：那就是处理和传递信息。

而程序，则用来决定，工作，以怎样的形式完成，仅此而已。

（所以上面才是重要程度的排序）
